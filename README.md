# daily-report
Daily Report Dashboard ---------------------- This project is a simple HTML dashboard that automatically loads data from a Google Sheet (CSV format). It allows filtering by date range and displays activities in a clean table layout. The report updates automatically whenever the Google Sheet is updated.
